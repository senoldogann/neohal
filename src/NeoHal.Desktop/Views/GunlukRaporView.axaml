<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:NeoHal.Desktop.ViewModels"
             x:Class="NeoHal.Desktop.Views.GunlukRaporView"
             x:DataType="vm:GunlukRaporViewModel">
    
    <ScrollViewer>
        <Grid RowDefinitions="Auto,Auto,Auto,*">
            
            <!-- Başlık ve Tarih Seçimi -->
            <Border Grid.Row="0" Background="{DynamicResource BackgroundMediumBrush}" Padding="20,15">
                <Grid ColumnDefinitions="*,Auto,Auto">
                    <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="15">
                        <TextBlock Text="📊 Günlük Rapor" FontSize="22" FontWeight="Bold" VerticalAlignment="Center"/>
                    </StackPanel>
                    
                    <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="10">
                        <TextBlock Text="Tarih:" VerticalAlignment="Center" Foreground="Gray"/>
                        <DatePicker SelectedDate="{Binding RaporTarihi}" Width="200"/>
                    </StackPanel>
                    
                    <Button Grid.Column="2" Content="🔄 Yenile" 
                            Command="{Binding RaporOlusturCommand}"
                            Padding="15,10" Margin="15,0,0,0"/>
                </Grid>
            </Border>
            
            <!-- Özet Kartları -->
            <Grid Grid.Row="1" ColumnDefinitions="*,*,*,*" Margin="20,20,20,10">
                <!-- Satış Özeti -->
                <Border Grid.Column="0" Background="{DynamicResource InfoBrush}" CornerRadius="10" Padding="20" Margin="0,0,10,0">
                    <StackPanel>
                        <TextBlock Text="💰 SATIŞ TOPLAMI" Foreground="#bbdefb" FontSize="11" FontWeight="Bold"/>
                        <TextBlock Text="{Binding ToplamSatisTutari, StringFormat=₺{0:N2}}" 
                                   FontSize="28" FontWeight="Bold" Foreground="White" Margin="0,10,0,0"/>
                        <TextBlock Text="{Binding ToplamFaturaSayisi, StringFormat='{}{0} fatura'}" 
                                   Foreground="#90caf9" FontSize="12"/>
                    </StackPanel>
                </Border>
                
                <!-- Giriş Özeti -->
                <Border Grid.Column="1" Background="{DynamicResource SuccessBrush}" CornerRadius="10" Padding="20" Margin="5,0">
                    <StackPanel>
                        <TextBlock Text="📦 MAL GİRİŞİ" Foreground="#c8e6c9" FontSize="11" FontWeight="Bold"/>
                        <TextBlock Text="{Binding ToplamGirisKg, StringFormat='{}{0:N0} kg'}" 
                                   FontSize="28" FontWeight="Bold" Foreground="White" Margin="0,10,0,0"/>
                        <TextBlock Text="{Binding ToplamIrsaliyeSayisi, StringFormat='{}{0} irsaliye'}" 
                                   Foreground="#a5d6a7" FontSize="12"/>
                    </StackPanel>
                </Border>
                
                <!-- Kasa Özeti -->
                <Border Grid.Column="2" Background="{DynamicResource WarningBrush}" CornerRadius="10" Padding="20" Margin="5,0">
                    <StackPanel>
                        <TextBlock Text="🏦 KASA BAKİYE" Foreground="#ffe0b2" FontSize="11" FontWeight="Bold"/>
                        <TextBlock Text="{Binding KasaBakiye, StringFormat=₺{0:N2}}" 
                                   FontSize="28" FontWeight="Bold" Foreground="White" Margin="0,10,0,0"/>
                        <StackPanel Orientation="Horizontal" Spacing="10">
                            <TextBlock Text="{Binding KasaGirisi, StringFormat='↑{0:N0}'}" 
                                       Foreground="#c8e6c9" FontSize="12"/>
                            <TextBlock Text="{Binding KasaCikisi, StringFormat='↓{0:N0}'}" 
                                       Foreground="#ffcdd2" FontSize="12"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
                
                <!-- Toplam KG -->
                <Border Grid.Column="3" Background="{DynamicResource FaturaColor}" CornerRadius="10" Padding="20" Margin="10,0,0,0">
                    <StackPanel>
                        <TextBlock Text="⚖️ SATILAN MAL" Foreground="#e1bee7" FontSize="11" FontWeight="Bold"/>
                        <TextBlock Text="{Binding ToplamKg, StringFormat='{}{0:N0} kg'}" 
                                   FontSize="28" FontWeight="Bold" Foreground="White" Margin="0,10,0,0"/>
                        <TextBlock Text="net ağırlık" Foreground="#ce93d8" FontSize="12"/>
                    </StackPanel>
                </Border>
            </Grid>
            
            <!-- Kesintiler Detayı -->
            <Border Grid.Row="2" Background="{DynamicResource BackgroundDarkBrush}" CornerRadius="10" Margin="20,10" Padding="20">
                <Grid ColumnDefinitions="*,*,*,*">
                    <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                        <TextBlock Text="KDV" Foreground="Gray" FontSize="11"/>
                        <TextBlock Text="{Binding ToplamKdv, StringFormat=₺{0:N2}}" 
                                   FontSize="18" FontWeight="SemiBold" Foreground="#64b5f6"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                        <TextBlock Text="RÜSUM" Foreground="Gray" FontSize="11"/>
                        <TextBlock Text="{Binding ToplamRusum, StringFormat=₺{0:N2}}" 
                                   FontSize="18" FontWeight="SemiBold" Foreground="#ffb74d"/>
                    </StackPanel>
                    <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                        <TextBlock Text="KOMİSYON" Foreground="Gray" FontSize="11"/>
                        <TextBlock Text="{Binding ToplamKomisyon, StringFormat=₺{0:N2}}" 
                                   FontSize="18" FontWeight="SemiBold" Foreground="#81c784"/>
                    </StackPanel>
                    <StackPanel Grid.Column="3" HorizontalAlignment="Center">
                        <TextBlock Text="GİRİŞ KAP" Foreground="Gray" FontSize="11"/>
                        <TextBlock Text="{Binding ToplamGirisKap, StringFormat='{}{0} adet'}" 
                                   FontSize="18" FontWeight="SemiBold" Foreground="#ba68c8"/>
                    </StackPanel>
                </Grid>
            </Border>
            
            <!-- Satış Detay Listesi -->
            <Border Grid.Row="3" Background="{DynamicResource BackgroundMediumBrush}" Margin="20,10,20,20" CornerRadius="10" Padding="15">
                <Grid RowDefinitions="Auto,*">
                    <TextBlock Grid.Row="0" Text="📋 Satış Detayı" FontWeight="Bold" FontSize="14" Margin="0,0,0,10"/>
                    
                    <DataGrid Grid.Row="1" 
                              ItemsSource="{Binding SatisDetay}"
                              AutoGenerateColumns="False"
                              IsReadOnly="True"
                              GridLinesVisibility="Horizontal"
                              BorderThickness="0"
                              CanUserResizeColumns="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Saat" Binding="{Binding Saat}" Width="70" MinWidth="50"/>
                            <DataGridTextColumn Header="Fatura No" Binding="{Binding FaturaNo}" Width="130" MinWidth="100"/>
                            <DataGridTextColumn Header="Müşteri" Binding="{Binding CariAdi}" Width="200" MinWidth="120"/>
                            <DataGridTextColumn Header="Miktar (kg)" Binding="{Binding Kg, StringFormat=N1}" Width="110" MinWidth="90"/>
                            <DataGridTextColumn Header="Tutar (₺)" Binding="{Binding Tutar, StringFormat=N2}" Width="120" MinWidth="90"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
            
        </Grid>
    </ScrollViewer>
</UserControl>

<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        x:Class="NeoHal.Desktop.Views.PrintPreviewWindow"
        Title="Yazdırma Önizleme"
        Width="500" Height="300"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource BackgroundDarkBrush}">
    
    <Grid RowDefinitions="*,Auto">
        <!-- İçerik -->
        <Border Grid.Row="0" Background="{DynamicResource BackgroundMediumBrush}" Padding="30" Margin="20">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="🖨️" FontSize="48" HorizontalAlignment="Center"/>
                <TextBlock Text="Yazdırma Önizlemesi" FontSize="20" FontWeight="Bold" Foreground="White" 
                           HorizontalAlignment="Center" Margin="0,15,0,10"/>
                <TextBlock Text="Yazdır butonuna tıklayarak belgeyi tarayıcınızda açabilir ve yazdırabilirsiniz." 
                           Foreground="#aaa" TextWrapping="Wrap" TextAlignment="Center" MaxWidth="350"/>
            </StackPanel>
        </Border>
        
        <!-- Butonlar -->
        <Border Grid.Row="1" Background="{DynamicResource BackgroundMediumBrush}" Padding="15">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Spacing="15">
                <Button Content="🖨️ Yazdır (Tarayıcıda Aç)" 
                        Click="OnPrintClick"
                        Background="#4CAF50" 
                        Foreground="White" 
                        Padding="25,12"
                        FontWeight="Bold"/>
                <Button Content="Kapat" 
                        Click="OnCloseClick"
                        Background="{DynamicResource BackgroundMediumBrush}" 
                        Foreground="White" 
                        Padding="25,12"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
